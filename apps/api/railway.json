{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install uv && (uv sync --frozen --no-dev || uv sync --no-dev)"
  },
  "deploy": {
    "startCommand": "uv run alembic upgrade head && uv run uvicorn dpp_api.main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips=*",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
